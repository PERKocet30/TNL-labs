/* Patch 067 — REVERTED. This patch briefly opened the labs (the group
   rooms) to logged-out visitors. Jorge's call: the rooms — the group
   chats especially — require an account. The Showroom, the Market, and
   the public /u /p /m pages stay public; the labs are the part of the
   network you join to enter.

   Zero hunks: the build applies nothing and the app is byte-identical
   to the 066 state. Kept as a numbered tombstone so the sequence stays
   contiguous and the decision is on the record. */
export default [];
