/* Patch 071 — REVERTED at Jorge's call: gating the autoplay observer
   behind the intro overlay didn't fix the glitching intro. Zero hunks —
   the build applies nothing. Kept as a numbered tombstone so the
   sequence stays contiguous. */
export default [];
